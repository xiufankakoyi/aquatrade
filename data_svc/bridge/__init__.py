"""
交互层 (Bridge Layer)

内存中的数据桥梁，负责数据格式标准化和零拷贝传输。
使用 Apache Arrow 作为数据交换格式。

架构位置:
┌─────────────────────────────────────────────────────────────────┐
│                      交互层 (Apache Arrow)                       │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ • 内存数据格式标准 (零拷贝共享)                               │ │
│  │ • Arrow Flight 协议 (高速数据传输)                           │ │
│  │ • Arrow IPC 格式 (进程间通信)                                │ │
│  │ • 统一数据桥梁 (连接写入层和分析层)                           │ │
│  │ • 支持 Arrow RecordBatch 流式处理                            │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
"""

from .arrow_bridge import ArrowBridge, get_arrow_bridge

__all__ = ['ArrowBridge', 'get_arrow_bridge']
