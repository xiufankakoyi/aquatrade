# Models

Local model and fine-tuning subprojects.

- `qwen_sentiment/`: local sentiment model/tokenizer assets.
- `finetune_hub/`: standalone fine-tuning UI/API project.

Large model weights and generated artifacts should stay ignored.
