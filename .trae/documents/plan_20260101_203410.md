# Deep Dive Performance Optimization Plan

## 1. Fix Limit Status Full Table Scan (Highest Priority)

### 1.1 Debug Date Filtering
- **Issue**: Limit_status table is still being scanned without filters (8.624s)
- **Fix**: Add debug logging to understand why our filtering isn't being applied
- **Implementation**:
  - Modify `load_to_polars_lazy` method to log the schema of the stock_limit_status table
  - Add debug logging to show the actual date column name being used
  - Verify that the where_clause is being correctly built for stock_limit_status
  - Check if the table has a different date column name than expected

### 1.2 Force Date Filter Application
- **Fix**: Add a direct date filter in the `load_limit_status` call from the main data loading function
- **Implementation**:
  - Modify `optimized_data_query.py` to explicitly pass date_column='trade_date' when calling load_to_polars for limit_status
  - Add a fallback mechanism that ensures date filters are always applied
  - Consider adding a view or materialized view for the limit_status table with proper indexing

## 2. Optimize Trading Dates Fetching

### 2.1 Fix Trading Dates Cache
- **Issue**: get_trading_dates is still taking 2.990s, cache not being hit
- **Fix**: Debug why the trading dates cache isn't being used
- **Implementation**:
  - Add debug logging to track cache hits and misses for trading dates
  - Verify that the cache_key is being generated correctly
  - Ensure the cache is being populated properly
  - Check if the `_all_trading_dates_cache` is being used correctly

### 2.2 Preload Trading Dates on Startup
- **Fix**: Preload trading dates when the database connection is initialized
- **Implementation**:
  - Modify the `__init__` method to preload trading dates in a background thread
  - Ensure the cache is available before the first get_trading_dates call
  - Add a flag to track when preloading is complete

## 3. Optimize Backtest Execution (Core Logic)

### 3.1 Profile Signal Generation
- **Issue**: Signal generation is taking 5616.25ms (34.25ms/天)
- **Fix**: Profile and optimize the signal generation logic
- **Implementation**:
  - Add detailed timing logs to identify which part of signal generation is slow
  - Consider vectorizing more operations using NumPy or Pandas
  - Check if there are any nested loops that can be optimized
  - Verify that Numba JIT is being applied correctly to all critical functions

### 3.2 Optimize Trade Matching
- **Issue**: The trade matching loop is taking 66.2ms, which is relatively low, but could be optimized further
- **Fix**: Review the trade matching algorithm for potential optimizations
- **Implementation**:
  - Ensure all trade matching functions are decorated with Numba JIT
  - Consider using more efficient data structures for trade matching
  - Profile the trade matching loop to identify any bottlenecks

## 4. Implement Result Caching

### 4.1 Cache Backtest Results
- **Fix**: Implement caching for backtest results to avoid redundant calculations
- **Implementation**:
  - Add a cache key based on strategy parameters and date range
  - Store full backtest results in a Redis or file-based cache
  - Add cache invalidation logic when new data is added

## Implementation Steps

1. **Debug Limit Status Filtering** (Highest Priority)
   - Add debug logging to understand why date filters aren't being applied
   - Modify the load_to_polars_lazy method to log the actual date column name
   - Verify the where_clause construction for stock_limit_status

2. **Fix Trading Dates Cache** (High Priority)
   - Add debug logging to track cache hits/misses
   - Fix cache population and retrieval logic
   - Implement preloading on startup

3. **Profile Signal Generation** (Medium Priority)
   - Add detailed timing logs to signal generation functions
   - Identify and optimize the slowest parts
   - Verify Numba JIT is being applied correctly

4. **Implement Result Caching** (Low Priority)
   - Add caching for backtest results
   - Implement proper cache invalidation

## Expected Results

- **Trading Dates Fetching**: Reduce from 2.990s to <0.1s
- **Limit Status Loading**: Reduce from 8.624s to <1s
- **Backtest Execution**: Reduce from 34.54s to <20s
- **Total Backtest Time**: Reduce from 37.81s to <25s

Let me start by debugging the limit_status filtering issue, which is causing the most significant slowdown in the data loading phase.