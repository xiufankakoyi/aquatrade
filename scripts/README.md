# Scripts

Reusable operational scripts.

- `start/`: local launch scripts.
- `legacy/`: older entry points kept for reference or manual migration.

Put one-off diagnostics in `sandbox/`, not here. Scripts in this directory should be safe to keep and explainable from their filename.
