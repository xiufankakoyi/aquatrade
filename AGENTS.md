# AQUATRADE Agent Rules

- 不要输出买入、卖出、仓位、止盈、止损等投资建议。
- 不要凭空编造公司业务、概念归属、收入占比或证据来源。
- 不要破坏现有回测、优化、Socket.IO、dashboard、portfolio、similarity 页面。
- 新增后端能力必须补 API 文档或 smoke test。
- 前端页面必须中文化，数值格式统一：百分比 2 位、分数 4 位、金额用 万/亿。
- 优先小步提交，避免跨模块大重构。
- 遇到字段缺失必须降级处理，不能让查询或页面直接崩溃。
- 概念映射默认以本地结构化证据为准；证据为空时显示“暂无本地证据”。
