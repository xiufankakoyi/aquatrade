# 清理项目根目录的平铺旧文件

## 目标
删除项目根目录下的旧平铺文件，保持项目结构整洁

## 删除文件
```bash
quant/ai_daily_brief.txt        # 旧文件，应在 data/cleaned_data/{日期}/ 下
quant/market_dashboard.csv      # 旧文件，应在 data/cleaned_data/{日期}/ 下
quant/stock_feature_matrix.csv  # 旧文件，应在 data/cleaned_data/{日期}/ 下
```

## 预期结果
项目根目录不再有平铺的数据文件

## 保留的输出目录
- `main.py` → `data/data_lake/{日期}/`
- `combined.py` → `data/cleaned_data/{日期}/`