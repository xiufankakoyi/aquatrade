# 修复状态查询 404 错误

## 问题根因

服务器返回 404 错误，因为 `server.py` 只实现了 `do_POST` 方法，但状态查询使用 GET 请求。

## 解决方案

在 `QuantRequestHandler` 类中添加 `do_GET` 方法来处理 GET 请求：

* 解析查询参数获取 job\_id

* 返回任务状态 JSON 响应

* 保持与 `handle_status_command` 相同的逻辑

## 修改文件

* `server.py` - 添加 `do_GET` 方法

