# Integrations

Standalone connector and integration projects.

- `mcp-aquatrade/`: AquaTrade MCP integration package.

Keep integration-specific package managers, build files, and connector code here. Shared application logic belongs in `core/`, `server/`, or `data_svc/`.
