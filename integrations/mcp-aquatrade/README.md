# mcp-aquatrade

Standalone MCP integration package for AquaTrade.

Install dependencies from this directory when working on the connector:

```powershell
cd integrations/mcp-aquatrade
npm install
```

Do not commit `node_modules/`.
