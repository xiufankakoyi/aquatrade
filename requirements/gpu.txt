# GPU 加速依赖（可选）
# 安装前请确保已安装 CUDA Toolkit (https://developer.nvidia.com/cuda-downloads)

# CuPy - NumPy 的 CUDA 版本
# 根据你的 CUDA 版本选择对应的包：
# CUDA 11.x: cupy-cuda11x
# CUDA 12.x: cupy-cuda12x
# 或者让 pip 自动检测: cupy-cuda11x 或 cupy-cuda12x
cupy-cuda11x>=12.0.0; sys_platform != "win32"  # Linux/Mac
# Windows 上可能需要从源码编译或使用 conda

# RAPIDS cuDF - pandas 的 GPU 版本
# 注意：RAPIDS 主要支持 Linux，Windows 支持有限
# cudf-cu11>=23.0.0; sys_platform == "linux"

# Numba - JIT 编译，支持 CUDA
numba>=0.57.0

# 注意：
# 1. Windows 上 GPU 加速支持有限，建议使用 Linux 或 WSL2
# 2. 需要先安装 CUDA Toolkit
# 3. 可以使用 conda 安装（推荐）：
#    conda install -c conda-forge cupy numba
#    conda install -c rapidsai -c conda-forge -c nvidia cudf

