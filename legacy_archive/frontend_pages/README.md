# 前端旧页面归档

归档日期：2026-06-11。

这些页面在归档前已通过 `myapp/src` 全局引用审计，未发现路由、静态 import 或动态 import。当前生效页面分别由 `DashboardOverview.vue`、`StrategyDetailKline.vue`、`StrategyCenterPage.vue` 和 `IndustryChainRadar.vue` 等承担。

归档不代表删除。若后续需要恢复，必须先明确路由、数据契约和统一状态组件，再移回 `myapp/src`。
